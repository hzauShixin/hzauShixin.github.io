# 个人网站更新操作指南

## 📁 项目位置

所有文件都在：`C:\quarto-site\my-website\`

---

## ✏️ 第一步：修改网页内容

用 Positron 打开并编辑以下文件：

| 文件 | 说明 |
|------|------|
| `index.qmd` | 首页内容 |
| `about.qmd` | 关于我页面 |
| `_quarto.yml` | 网站标题、导航栏、主题等配置 |
| `styles.css` | 网页样式（颜色、字体等） |

修改完成后按 **Ctrl + S** 保存文件。

---

## 🔨 第二步：在 Positron 中打开终端

在 Positron 顶部菜单点击 **Terminal（终端）→ New Terminal**，
底部会弹出一个终端面板（默认是 PowerShell）。

---

## 🖥️ 第三步：渲染网站（生成 HTML）

在终端中逐行输入以下命令，每行按 **Enter** 执行：

```powershell
$env:TEMP = "C:\quarto-tmp"
$env:TMP  = "C:\quarto-tmp"
$env:PATH += ";D:\Positron\resources\app\quarto\bin"
Set-Location "C:\quarto-site\my-website"
quarto render
```

✅ 看到 `Output created: docs\index.html` 表示渲染成功。

---

## 📤 第四步：提交并推送到 GitHub

继续在同一个终端面板输入：

```powershell
git add .
git commit -m "Update website content"
git pull origin main --rebase
git push origin main
```

✅ 看到 `main -> main` 表示推送成功。

---

## 🌐 第五步：查看更新后的网站

等待约 **1~2 分钟**，然后访问：

👉 **https://hzauShixin.github.io/**

刷新页面即可看到最新内容。

---

## ⚡ 一键发布（更简单的方式）

如果嫌以上步骤麻烦，直接**双击** `publish.bat` 文件，
它会自动完成第二步和第三步的所有操作！

---

## ⚠️ 注意事项

1. **每次打开新的终端窗口都需要重新设置 TEMP/TMP**，
   因为这些设置只在当前终端会话中生效。

2. **首次推送需要 GitHub 认证**：
   - 弹出登录窗口时，用户名填 `hzauShixin`
   - 密码填 **Personal Access Token**（不是 GitHub 网站密码）
   - Token 在此创建：https://github.com/settings/tokens/new（勾选 `repo`）
   - 输入一次后 Windows 会记住，以后无需再输入

3. **`.qmd` 文件格式**：内容使用 Markdown 语法编写，
   例如 `**粗体**`、`# 标题`、`- 列表` 等。

---

## 📝 Markdown 常用语法速查

```markdown
# 一级标题
## 二级标题
### 三级标题

**粗体文字**
*斜体文字*

- 无序列表项1
- 无序列表项2

1. 有序列表项1
2. 有序列表项2

[链接文字](https://网址)

![图片描述](图片路径)

| 表头1 | 表头2 |
|-------|-------|
| 内容1 | 内容2 |
```
